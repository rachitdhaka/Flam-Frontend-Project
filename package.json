{
  "name": "collaborative-canvas",
  "version": "1.0.0",
  "description": "Real-time collaborative drawing canvas with WebSocket synchronization",
  "main": "server/server.js",
  "scripts": {
    "build": "tsc && cp client/index.html dist/client/ && cp client/style.css dist/client/ && cp client/app.js dist/client/",
    "start": "node dist/server/server.js",
    "dev": "npm run build && npm start",
    "watch": "tsc --watch"
  },
  "keywords": [
    "canvas",
    "websocket",
    "collaborative",
    "drawing"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.6.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3"
  }
}
